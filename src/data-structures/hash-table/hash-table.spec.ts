import { HashTable } from "./hash-table";

describe("hash table", () => {
  it.todo("should set a key and a value in the hash table");
  it.todo("should get a value from the hash table, given a key as argument");
  it.todo("should hash a key idepontently");
});
